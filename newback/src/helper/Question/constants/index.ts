export const EntityName = "Question";

export const routePath = "question";

export enum ErrorMessages {
  NOT_FOUND = "Question not found",
  QUESTION_SHOULD_BE_NUMERIC = "Question should be numeric"
}

export enum paths {}
