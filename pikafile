задокументирую тут последовательность действий для ясности: 
1. создал локальную ветку из удаленной с таким же именем для того чтобы похимичить  git checkout origin/test-branch-for-rebase -b test-branch-for-rebase
2. у меня там было 2 коммита в ветке, сделал ребейз  git rebase -i HEAD~2 (если 3 коммита склеиваем то HEAD~3)
3. поменял имена если нужно у сообщений коммитов (указав параметр r в редакторе перед коммитом вместо pick)
4. объединил коммит, через команду  git rebase -i HEAD~2 с указанием параметра s перед вторым коммитом (второй в редакторе, так как там не как в гитлоге а в обратном порядке коммиты) и удалил сообщение второго коммита.
5. git commit --ammend для изменения сообщения коммита, оставляем что есть, так как у нас оно одно с предыдущего шага.
6. 